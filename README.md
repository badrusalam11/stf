# Installation
When you apply change, build the dokcer image with :  docker build -t custom/stf .
To build, do: 
1. docker-compose down (to turn of the container)
2. docker-compose build
3. docker-compose up

# Shortcut
For simplicity, run: ./deploy or source deploy.sh